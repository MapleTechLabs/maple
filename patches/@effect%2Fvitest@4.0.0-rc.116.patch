diff --git a/dist/internal/internal.js b/dist/internal/internal.js
index 25ab056801ac57955d759df03f2ed041a008fcdd..b752e651638d783d182df26ec1fff1ba2b6beb62 100644
--- a/dist/internal/internal.js
+++ b/dist/internal/internal.js
@@ -1,6 +1,7 @@
 /**
  * @since 4.0.0
  */
+import { getCurrentSuite } from "@vitest/runner";
 import * as Cause from "effect/Cause";
 import * as Duration from "effect/Duration";
 import * as Effect from "effect/Effect";
@@ -13,7 +14,6 @@ import * as TestClock from "effect/testing/TestClock";
 import * as TestConsole from "effect/testing/TestConsole";
 import * as Arbitrary from "effect/unstable/arbitrary/Arbitrary";
 import * as V from "vitest";
-const getCurrentSuite = V.TestRunner.getCurrentSuite;
 const runPromise = /*#__PURE__*/Effect.fnUntraced(function* (effect, _ctx) {
   const exit = yield* Effect.exit(effect);
   if (Exit.isFailure(exit)) {
@@ -29,7 +29,8 @@ const runPromise = /*#__PURE__*/Effect.fnUntraced(function* (effect, _ctx) {
 /** @internal */
 const runTest = ctx => effect => {
   const promise = runPromise(effect, ctx);
-  if (ctx) {
+  // Patched (maple): Bun's `bun test` context has no `signal`.
+  if (ctx?.signal) {
     // Vitest stops awaiting the test promise once the signal aborts (timeout or
     // cancellation), so only then add a hook that waits for the finalizers.
     // Registering it unconditionally would change the teardown of every test.
diff --git a/src/internal/internal.ts b/src/internal/internal.ts
index 3e001b19836fb8169155da1b6c70bffb38cdb0c7..0752377e9f26198ad3823ca83b56fd987850fe31 100644
--- a/src/internal/internal.ts
+++ b/src/internal/internal.ts
@@ -2,6 +2,7 @@
  * @since 4.0.0
  */
 
+import { getCurrentSuite } from "@vitest/runner"
 import * as Cause from "effect/Cause"
 import * as Duration from "effect/Duration"
 import * as Effect from "effect/Effect"
@@ -17,7 +18,6 @@ import * as Arbitrary from "effect/unstable/arbitrary/Arbitrary"
 import * as V from "vitest"
 import type * as Vitest from "../index.ts"
 
-const getCurrentSuite = V.TestRunner.getCurrentSuite
 
 const runPromise: <E, A>(
   _: Effect.Effect<A, E, never>,
@@ -36,7 +36,8 @@ const runPromise: <E, A>(
 /** @internal */
 const runTest = (ctx?: Vitest.TestContext) => <E, A>(effect: Effect.Effect<A, E>) => {
   const promise = runPromise(effect, ctx)
-  if (ctx) {
+  // Patched (maple): Bun's `bun test` context has no `signal`.
+  if (ctx?.signal) {
     // Vitest stops awaiting the test promise once the signal aborts (timeout or
     // cancellation), so only then add a hook that waits for the finalizers.
     // Registering it unconditionally would change the teardown of every test.
