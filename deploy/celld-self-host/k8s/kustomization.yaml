apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: maple
resources:
  - namespace.yaml
  - configmap.yaml
  - minio.yaml
  - proxy.yaml
  - api.yaml
  - sync.yaml
  - alerting.yaml
  - web.yaml
  - caddy.yaml
# Own S3: delete minio.yaml and set CELLD_BUCKET / S3_ENDPOINT / AWS_* on the
# celld Deployments. Own Postgres/CH/Electric: set URLs on maple-selfhost ConfigMap.
